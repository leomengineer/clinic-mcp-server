# Same Postgres/pgvector instance as docs-rag-chatbot (chunks table already populated).
DATABASE_URL=postgresql://rag:rag@localhost:5432/rag

# Anti-hallucination gate for search_clinic_docs (dense cosine similarity).
SIMILARITY_FLOOR=0.35

# Clinic timezone for appointment date windows (IANA name).
CLINIC_TZ=America/Los_Angeles
